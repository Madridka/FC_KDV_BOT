version: "3.9"

services:
  telegram-bot:
    build: .
    container_name: kdv-telegram-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./static:/app/static:ro
